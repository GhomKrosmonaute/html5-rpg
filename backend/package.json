{
  "scripts": {
    "start": "tsc && node dist/index.js",
    "format": "prettier --write src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prettier": {
    "semi": false
  }
}
